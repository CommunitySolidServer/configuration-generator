import { Choices } from '../choices/choices';
/**
 * Generates all the necessary overrides and other components needed when specific options are chosen.
 */
export declare function generateBody(choices: Choices): unknown[];
