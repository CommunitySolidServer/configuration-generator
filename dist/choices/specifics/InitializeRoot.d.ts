import { BooleanOption, Choice } from '../Choice';
/**
 * Initialize root as storage and fully accessible (GIVES FULL ACCESS WHICH NEEDS TO BE UPDATED).
 */
export declare const INITIALIZE_ROOT: Choice<BooleanOption>;
