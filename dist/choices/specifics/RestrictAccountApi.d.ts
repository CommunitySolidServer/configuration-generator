import { BooleanOption, Choice } from '../Choice';
/**
 * Also apply authorization rules to IDP API (GIVES FULL ACCESS WHICH NEEDS TO BE UPDATED).
 */
export declare const RESTRICT_ACCOUNT_API: Choice<BooleanOption>;
